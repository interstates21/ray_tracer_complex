#include "rt.h"

int			listen_controls(int keycode, t_cam *cam)
{
	if (keycode == K_LEFT)
    {
        cam->forward = cam->forward
    }
    if (keycode == K_RIGHT)
    {
        
    }

	return (0);
}
